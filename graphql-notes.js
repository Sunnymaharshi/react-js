/*
    GraphQL 
        apollo client
            used at client side to call GraphQL apis 
            create client & pass it to ApolloProvider in App component
            gql is used write queries & mutations
            useQuery hook is used to run the queries 
                gives error,loading & data 
            useMutation hook is used to run the mutations
                given mutation function and error
        apollo server 
            used in server to create GraphQL queries & mutations 
            typedefs 
                to define types for queries & mutations 
            resolvers 
                actions for queries & mutations
*/
